PHPCS-PHP

PHP interface to PHPCS

Use results from PHPCS inside your own PHP programs.

Usage:
$phpcs = new PHPCS( "/path/to/check" );
$results = $phpcs->generate_report();
$results->_print();
or print_r( $results ); (you can use the array however you like)
